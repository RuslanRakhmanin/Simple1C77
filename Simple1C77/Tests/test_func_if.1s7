Перем б;

// Комментарий 01
Процедура Alpha(a,z,x)
  Перем b;

  a = 2;
  b = a + 1;
КонецПроцедуры;

Функция Beta(f)
  c = f + 4; // c = 0
  f = 222;
  Возврат f + 4;
КонецФункции;

Функция Текст(т) Экспорт
  Возврат т + " done";
КонецФункции;

b = 100 + 10 + 1; // c + 1
Alpha(b, Beta(b - 1), 10);
d = Beta(b);
text1 = "All";
text2 = "Nothing";
Если НЕ (b = 222 ИЛИ (б > 300 ИЛИ b <= -300 )) Тогда
  outtext = Текст(text1);
ИначеЕсли НЕ (b <> 222) Тогда
  outtext = Текст(text2);
КонецЕсли;
